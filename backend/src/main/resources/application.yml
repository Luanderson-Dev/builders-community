spring:
  application:
    name: togetherdev-space-api
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/togetherdev}
    username: ${SPRING_DATASOURCE_USERNAME:togetherdev}
    password: ${SPRING_DATASOURCE_PASSWORD:togetherdev}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true

server:
  port: ${PORT:8080}

# Porta e credenciais padrão para o ambiente local (docker-compose.local.yml).
